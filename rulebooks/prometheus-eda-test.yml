---
- name: Prometheus EDA Test
  hosts: all

  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000

  rules:
    - name: Trigger AAP from Prometheus
      condition: >
        event.payload.alerts is defined and
        event.payload.alerts[0].labels.alertname == "EDATestAlert"
      action:
        debug:
