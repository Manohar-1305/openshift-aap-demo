---
- name: Kubernetes Pod Crash Diagnosis
  hosts: all

  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000

  rules:
    - name: Diagnose Pod Crash
      condition: >
        event.payload.alerts is defined and
        event.payload.alerts[0].status == "firing" and
        event.payload.alerts[0].labels.alertname == "PodCrashLoopBackOff"
      action:
        run_job_template:
          name: "Kubernetes Pod Crash Diagnosis"
          organization: "Default"
          job_args:
            extra_vars:
              alert_name: "{{ event.payload.alerts[0].labels.alertname }}"
              namespace: "{{ event.payload.alerts[0].labels.namespace }}"
              pod_name: "{{ event.payload.alerts[0].labels.pod }}"
